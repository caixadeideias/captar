= form_for [parent, resource] do |f|
  - if resource.errors.any?
    #error_explanation
      h2
        = pluralize(resource.errors.count, "erro")
        | impede(m) a funcionalidade de ser salva:
      ul
        - resource.errors.full_messages.each do |msg|
          li = msg
  .field
    = f.label :title
    br
    = f.text_field :title
  .field
    = f.label :points
    br
    = f.number_field :points
  .field
    = f.label :importance
    br/
    = f.number_field :importance
  .actions
    = f.submit
